# Examples

Three examples. No framework cosplay.

## `basic/.deva`

Default per-project settings with a couple of common mounts.

## `rust/.deva`

Use the rust profile and keep a couple of useful developer env vars.

## `isolated-auth/.deva`

Show how to keep auth state separate instead of reusing your default home for
everything.
